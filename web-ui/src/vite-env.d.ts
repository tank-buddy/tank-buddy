/// <reference types="vite/client" />

/**
 * The release this bundle was built from, injected by vite.config.ts. The page
 * that is running *is* the installed version, so nothing has to be read back
 * from the device to know what it has.
 */
declare const __UI_VERSION__: string
